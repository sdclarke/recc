# RECC SAMPLE CONFIG SAMPLE/TEST
# Any line starting with pound/space/empty will be ignored.
# Key/value pairs defined as KEY = VALUE
# KEYS can be lowercase, or uppercase.

SERVER=localhost:99999
CAS_SERVER=localhost:66666

verbose=1

tmpdir=/tmp/dir
 tmpdir=shouldnotbefound
#tmpdir=commentedout
auth_unconfigured_msg=authentication not configured
remote_platform_arch=x86_64
remote_platform_OSFamily=linux
deps_env_Header=/usr/local/bin
remote_env_Header=/usr/bin
remote_platform_Docker_Container=docker
